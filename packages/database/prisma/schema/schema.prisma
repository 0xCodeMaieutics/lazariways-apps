// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  schemas  = ["public", "lazari_lingo"]
}

generator client {
  provider = "prisma-client"
  output   = "../generated"
}
