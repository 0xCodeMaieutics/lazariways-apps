# UI conventions

This project uses `@workspace/ui` package for importing components.

# Example importing Button component

```tsx
import { Button } from "@workspace/ui/components/button"
```

# Form

Use [react-hook-form](https://react-hook-form.com/docs) and `@hookform/resolvers` libraries.
