## Clean Code

### Variable naming conventions

Avoid abbreviations; prefer descriptive, intention-revealing names.

Examples:

```
amt → amount
qty → quantity
dest → destination
src → source
errMsg → errorMessage
```
